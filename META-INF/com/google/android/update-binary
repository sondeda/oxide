#!/sbin/sh
SKIPUNZIP=1
unzip -o "$ZIPFILE" 'module.prop' -d "$MODPATH" >&2
unzip -o "$ZIPFILE" 'zygisk/*'   -d "$MODPATH" >&2
set_perm_recursive "$MODPATH" root root 0755 0644
ui_print "- Oxide Cheat installed"
ui_print "- Reboot to activate"
